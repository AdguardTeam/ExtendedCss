import { ExtendedCss } from './extended-css';

/**
 * Default export of ExtendedCss is needed for debug purposes
 * for beautiful usage of IIFE scripts
 * check "Debugging extended selectors" in README.md,
 * e.g. `ExtendedCss.query()`.
 *
 * Used for building `extended-css.js` and `extended-css.min.js`.
 */
export default ExtendedCss;
